services:
  web:
    build: .
    ports:
      - "${HOST_PORT:-8877}:6000"
    volumes:
      - .:/app
    environment:
      - DENON_IP=${DENON_IP}
    env_file:
      - .env
    restart: unless-stopped
